export type RIGOBTNPROPS = {
    inv : boolean,
    text : string,
    chevronColor?: string
}  

export type WWSCardProps = {
    title: string,
    date: string,
    desc?: string,
    volume?: string,
}

export type logoPillProps = {
    text: string
}